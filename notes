- www.pythonanywhere.com:
https://www.pythonanywhere.com/user/Oleksii/

- pip/pip3 install pandas - library for data processing;
- pip/pip3 install ipython - nice terminal for working with big data;
- pip/pip3 install xlrd
- python3 -m pip install --upgrade pip
  python3 -m pip install jupyter
  jupiter notebook
- pip/pip3 install geopy (gel long and latitude from the usual coords)
- pip/pip3 install opencv-python

samples:
df1 = pandas.DataFrame([[2,4,5],[10,20,30]], columns=["Price", "Age", "Value"], index:["..."])

- type(df1)
- dir(df1)
- df1.Price.max()
- pandas.read_csv || read_... JSON/Excel/txt [separators aspect]
- set header row: df8 = pandas.read_csv("data.txt", header = NONE)
  df.columns = ["...", "..."]
  - df9 = df8.set_index("ID")
  - drop = FALSE => columns is not deleted
  - label based indexing => df7.loc["row and column params"] + mybe used list func
  - index based indexing => df7.index[1:3, 1:3]
  - delete => df7.drop("..." / df7.index[0:3], 0)